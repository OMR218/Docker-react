sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t omar21/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true omar21/docker-react npm run test